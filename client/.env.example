# Если backend на другом хосте/порту — укажите полный URL API (без завершающего слэша).
# По умолчанию в dev используется прокси Vite — можно оставить пустым или указать:
VITE_API_URL=http://localhost:3001
